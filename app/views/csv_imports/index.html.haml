%h1
  Импорты из .csv файлов:
  = link_to 'Создать', new_csv_import_path, class: 'btn btn-success'

%table.table.table-stripped.table-bordered
  %thead
    %tr
      %th Файл
      %th Загружено(дата)
      %th
      %th
      %th

  %tbody
    - @csv_imports.each do |csv_import|
      %tr
        %td= csv_import.file
        %td= csv_import.proccessed_at
        %td= link_to 'Показать', csv_import, class: 'btn btn-default'
        %td= link_to 'Удалить', csv_import, :method => :delete, :data => { :confirm => 'Are you sure?' }, class: 'btn btn-danger'
